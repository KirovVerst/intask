(function () {
  'use strict';

  angular
    .module('application.tasks', [
      'application.tasks.controllers',
      'application.tasks.services'
    ]);

    angular
        .module('application.tasks.controllers', []);

    angular
        .module('application.tasks.services', []);

})();
